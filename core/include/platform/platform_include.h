#ifndef PLATFORM_INCLUDE

#include <arch/arch.h>
#include <chipset/chipset.h>

#define PLATFORM_INCLUDE(__subpath)		<platform/ARCH_PATH-CHIPSET_PATH/__subpath>
#define PLATFORM_SOURCE_INCLUDE(__subpath)	<platform/ARCH_PATH-CHIPSET_PATH/__subpath>

#endif

